{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "NEXT_PUBLIC_*",
        "PAYLOAD_SECRET",
        "DATABASE_URL",
        "BLOB_READ_WRITE_TOKEN",
        "OPENAI_API_KEY",
        "OPENAI_MODEL",
        "TRACKER_API_URL",
        "TRACKER_API_TOKEN",
        "NEXTJS_ENV"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "deploy": {
      "cache": false,
      "dependsOn": ["build"]
    }
  }
}
